//! The `:` line. Splits what was typed into words and parses them with the
//! same clap definitions as the shell, so `:open` and `indexer open` are one
//! command. A command that needs a project defaults to the selected one.
